[tools]
uv = "latest"

[tasks.setup]
run = "uv sync"
description = "Install all dependencies into .venv"

[tasks.format]
run = "uv run ruff format fintrack/ web_spending/ web_finances/ tests/ spending.py finances.py"
description = "Format code with ruff"

[tasks.format-check]
run = "uv run ruff format --check fintrack/ web_spending/ web_finances/ tests/ spending.py finances.py"
description = "Check code formatting"

[tasks.lint]
run = "uv run ruff check fintrack/ web_spending/ web_finances/ tests/ spending.py finances.py"
description = "Lint with ruff"

[tasks.lint-fix]
run = "uv run ruff check --fix fintrack/ web_spending/ web_finances/ tests/ spending.py finances.py"
description = "Fix lint issues with ruff"

[tasks.test]
depends = ["format-check", "lint"]
run = [
    "uv run pytest tests/ -v --cov=fintrack --cov-report=term-missing -m 'not e2e'",
    "uv run pytest tests/ -v -m e2e",
]
description = "Run all CI checks (format, lint, unit, e2e for both apps)"

[tasks.test-unit]
run = "uv run pytest tests/ -v --cov=fintrack --cov-report=term-missing -m 'not e2e'"
description = "Run unit tests for both apps with coverage"

[tasks.test-e2e]
run = "uv run pytest tests/ -v -m e2e"
description = "Run e2e browser tests with Playwright (run playwright-install first)"

[tasks.playwright-install]
run = "uv run playwright install chromium"
description = "Install Playwright browser binaries"

[tasks.serve-spending]
run = "uv run python -m web_spending.app"
description = "Start the spending web server (port 5002)"

[tasks.serve-finances]
run = "uv run python web_finances/app.py"
description = "Start the finances web server (port 5001)"
