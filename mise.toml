[tools]
uv = "latest"

[tasks.setup]
run = "uv sync"
description = "Install all dependencies into .venv"

[tasks.format]
run = "uv run ruff format fintrack/ web/ tests/ fintrack.py"
description = "Format code with ruff"

[tasks.format-check]
run = "uv run ruff format --check fintrack/ web/ tests/ fintrack.py"
description = "Check code formatting"

[tasks.lint]
run = "uv run ruff check fintrack/ web/ tests/ fintrack.py"
description = "Lint with ruff"

[tasks.lint-fix]
run = "uv run ruff check --fix fintrack/ web/ tests/ fintrack.py"
description = "Fix lint issues with ruff"

[tasks.test]
depends = ["format-check", "lint"]
run = [
    "uv run pytest tests/ -v --cov=fintrack --cov-report=term-missing -m 'not e2e'",
    "uv run pytest tests/ -v -m e2e",
]
description = "Run all CI checks (format, lint, unit, e2e for both apps)"

[tasks.test-unit]
run = "uv run pytest tests/ -v --cov=fintrack --cov-report=term-missing -m 'not e2e'"
description = "Run unit tests for both apps with coverage"

[tasks.test-e2e]
run = "uv run pytest tests/ -v -m e2e --browser ${E2E_BROWSER:-chromium}"
description = "Run e2e browser tests (set E2E_BROWSER=webkit|firefox|chromium; default chromium)"

[tasks.test-e2e-webkit]
run = "E2E_BROWSER=webkit uv run pytest tests/ -v -m e2e --browser webkit"
description = "Run e2e browser tests against WebKit (Safari engine; run playwright-install-webkit first)"

[tasks.playwright-install]
run = "uv run playwright install chromium"
description = "Install the Chromium Playwright browser binary"

[tasks.playwright-install-webkit]
run = "uv run playwright install webkit"
description = "Install the WebKit (Safari engine) Playwright browser binary"

[tasks.migrate]
run = "uv run alembic upgrade head"
description = "Run database migrations to latest"

[tasks.serve]
run = "uv run python -m web.app"
description = "Start the fintrack web server (port 5003)"
